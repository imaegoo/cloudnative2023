<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nineya.springboot.mapper.LowcodeUserMapper">
    <!-- application.yml中type-aliases-package:未配置则需要用全名-->
    <select id="queryByUsername" resultType="com.nineya.springboot.entity.LowcodeUser">
        SELECT *
        FROM lowcode_users
        where username = #{username}
    </select>

    <insert id="insert" parameterType="com.nineya.springboot.entity.LowcodeUser">
        insert into lowcode_users
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null and id != ''">
                id,
            </if>
            <if test="username != null and username != ''">
                username,
            </if>
            <if test="password != null and password != ''">
                password,
            </if>
            <if test="email != null and email != ''">
                email,
            </if>
            <if test="createdTime != null">
                created_time,
            </if>
            <if test="updateTime != null">
                update_time,
            </if>
            <if test="isdeleted != null and isdeleted != ''">
                isdeleted,
            </if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="id != null and id != ''">
                #{id,jdbcType=NUMBERIC},
            </if>
            <if test="username != null and username != ''">
                #{username,jdbcType=VARCHAR},
            </if>
            <if test="password != null and password != ''">
                #{password,jdbcType=VARCHAR},
            </if>
            <if test="email != null and email != ''">
                #{email,jdbcType=VARCHAR},
            </if>
            <if test="createdTime != null">
                #{createdTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updateTime != null ">
                #{updateTime,jdbcType=TIMESTAMP},
            </if>
            <if test="isdeleted != null and isdeleted != ''">
                #{isdeleted,jdbcType=NUMBERIC},
            </if>
        </trim>
    </insert>
</mapper>