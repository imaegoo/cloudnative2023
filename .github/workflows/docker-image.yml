name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker login --username=mail1st@qq.com --password ${{secrets.ali_acr_token}} registry.cn-hangzhou.aliyuncs.com && bash ./docker-build-server.sh && bash ./docker-build-web.sh
